# Example .env for local development
# Replace values with your credentials. Do NOT commit real secrets.

# Postgres DB (optional - you already have this)
# DATABASE_URL=postgresql://postgres:animesh@localhost:5432/okil_ai_db

# SMTP (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=okilai.np@gmail.com
SMTP_PASS=suwx gnxr rdte dhsf
SMTP_FROM=okilai.np@gmail.com

# Frontend URL used in reset links
FRONTEND_URL=http://localhost:3000
